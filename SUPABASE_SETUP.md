# Supabase 설정 가이드 (현재 운영 구조)

현재 시스템은 아래 구조로 동작합니다.

- **DB**: Supabase (`reservations` 테이블)
- **파일 업로드**: Cloudflare Worker → Cloudflare R2
- **파일 URL 저장 위치**: Supabase `doc_url_1~3`

---

## 1. config.js 값

`config.js`에서 아래 값을 확인합니다.

- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`
- `R2_UPLOAD_WORKER_URL`
- `R2_UPLOAD_SECRET` (선택)
- `ADMIN_PASSWORD`

> `R2_UPLOAD_WORKER_URL`이 비어 있으면 업로드 시  
> `config.js에 R2_UPLOAD_WORKER_URL을 설정해 주세요.` 오류가 납니다.

---

## 2. Supabase RLS (reservations 테이블)

현재 파일은 R2에 저장되므로, Supabase Storage 정책은 필요 없습니다.  
필요한 것은 `reservations` 테이블 정책만입니다.

```sql
alter table public.reservations enable row level security;

drop policy if exists "Allow anon insert" on public.reservations;
create policy "Allow anon insert"
on public.reservations for insert to anon
with check (true);

drop policy if exists "Allow anon select" on public.reservations;
create policy "Allow anon select"
on public.reservations for select to anon
using (true);
```

차량 중량/예약 소요시간 분리 저장을 위해 컬럼이 없으면 아래 쿼리를 추가 실행합니다.

```sql
alter table public.reservations
add column if not exists vehicle_tonnage numeric;

alter table public.reservations
add column if not exists reservation_duration_minutes integer default 30;

alter table public.reservations
add column if not exists duration_mode text;

alter table public.reservations
add column if not exists recommended_duration_minutes integer;

alter table public.reservations
add column if not exists recommended_reasons jsonb;
```

---

## 3. Cloudflare Worker/R2 점검 항목

아래 3개가 맞아야 업로드가 성공합니다.

1. Worker가 배포되어 있고 URL이 열려야 함  
   - 예: `https://reservation-r2-upload.<subdomain>.workers.dev`
2. Worker에 R2 버킷 바인딩  
   - 바인딩명: `BUCKET`
   - 버킷명: `reservation-files`
3. R2 공개 URL 접두어 설정  
   - `R2_PUBLIC_BASE_URL`
   - 예: `https://pub-xxxxxxxx.r2.dev`

---

## 4. reservations 테이블 스키마 (실사용)

| 컬럼 | 타입 | 비고 |
|---|---|---|
| id | bigint | PK, generated by default as identity |
| created_at | timestamptz | default now() |
| reservation_date | date | 예약일 |
| reservation_time | text | 예: "08:30" |
| company_name | text | 업체명 |
| car_number_1 | text | 차량번호1 |
| car_number_2 | text | nullable |
| vehicle_tonnage | numeric | 차량 총중량(톤), nullable |
| reservation_duration_minutes | integer | 예약 소요 시간(분), 30/60 |
| duration_mode | text | 'auto' / 'manual' |
| recommended_duration_minutes | integer | 시스템 권장 소요 시간(분) |
| recommended_reasons | jsonb | 권장 근거 배열(JSON) |
| material_info | text | 자재 정보 |
| contact | text | 연락처 |
| doc_url_1 | text | 서류 URL 1 (R2 공개 URL) |
| doc_url_2 | text | 서류 URL 2 (R2 공개 URL) |
| doc_url_3 | text | 서류 URL 3 (R2 공개 URL) |
| status | text | '대기' / '승인' / '반려' |
| person_info | text | 출입 인원 정보(이름/명단 텍스트) |

---

## 5. 로컬 확인 순서

1. 로컬 서버 실행 (`python -m http.server 8080`)
2. `index.html`에서 예약 제출
3. 제출 성공 후 Supabase `reservations`에 `doc_url_1~3` 저장 확인
4. `admin.html`에서 첨부 링크 클릭 시 문서 열람 확인

---

## 참고

- 프로젝트의 `supabase/functions/upload-reservation-file`는 현재 사용하지 않습니다.
- 현재 기준 업로드 경로는 **Cloudflare Worker**입니다.
